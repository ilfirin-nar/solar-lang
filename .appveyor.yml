# Via AppVeyor

skip_commits:
  message: /((Create.*\.md)|(Update.*\.md))/

version: 1.0.{build}

configuration: Release

before_build:
  - echo HELLO!
  - cd src
  - nuget restore
  - cd ..

build:
  project: src\main.sln
  verbosity: normal
