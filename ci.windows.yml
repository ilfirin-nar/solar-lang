skip_commits:
  message: /((Create.*\.md)|(Update.*\.md))/

version: 1.0.{build}

configuration: Release

before_build:
  - cd src
  - nuget restore

build:
  project: src\main.sln
  verbosity: normal
